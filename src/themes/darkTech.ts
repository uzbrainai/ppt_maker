import { buildTheme } from "./buildTheme.js";
import { THEME_DEFS } from "./defs.js";

/** dark.tech — dark product/keynote theme (default appearance: dark). */
export const darkTech = buildTheme("dark.tech", THEME_DEFS["dark.tech"].dark, "dark");
