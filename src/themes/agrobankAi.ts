import { buildTheme } from "./buildTheme.js";
import { THEME_DEFS } from "./defs.js";

/** agrobank.ai — green agritech/fintech theme (default appearance: light). */
export const agrobankAi = buildTheme("agrobank.ai", THEME_DEFS["agrobank.ai"].light, "light");
