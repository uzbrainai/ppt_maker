# Where the admin SPA talks to the slidewind backend.
# In prod (Docker), nginx proxies /api to the slidewind service.
# For local dev outside Docker, point at your running backend:
VITE_API_BASE=/api
VITE_BASE=/
